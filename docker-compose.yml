version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      # Map localhost port 8080 to container port 80
      - "8080:80"
    restart: unless-stopped
